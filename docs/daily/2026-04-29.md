# Daily Summary 2026-04-29

**Commits:** 7  
**Contributors:** Moritz Laass

---

## akkado/ — 7 files touched

- **Phase 2.1: custom-property accessor + bend/aftertouch/dur transforms** (`f8dca4dd`)  
  Implements PRD §11.1–§11.2: unrecognized record-suffix keys (e.g. `{cutoff:0.3}`) now get compile-time slots in `SequenceCompiler`, surfaced via the new `SEQPAT_PROP` opcode. `bend()` and `aftertouch()` reuse the same slot plumbing; `dur()` multiplies `event.duration` in place. Touches `builtins.hpp`, `codegen.hpp`, `typed_value.hpp`, `codegen.cpp`, `codegen_patterns.cpp`, `typed_value.cpp`.

- **Phase 2 audit: close coverage gaps** (`1c4511b7`)  
  Adds exact-MIDI assertions for `duck`/`root` voicing modes, tightens `below`/`above` bounds, and adds a `dur` key propagation test. `test_codegen.cpp` extended with 102 new lines; final suite is 519 akkado / 153 cedar cases.

## cedar/ — 6 files touched

- **Phase 2.1 runtime support** (`f8dca4dd`)  
  Adds `SEQPAT_PROP` opcode (id 157) to `instruction.hpp` and `vm.cpp` dispatch. `cedar::Event` and `OutputEvents::OutputEvent` each gain 16 bytes (`MAX_PROPS_PER_EVENT=4`). New `sequencing.hpp` slab and `opcode_metadata.hpp` update. 144 new VM test lines in `test_vm.cpp`.

## experiments/ — 3 files touched

- **phase21_smoke.akk** (`f8dca4dd`)  
  Smoke-test script for the Phase 2.1 pipe-binding accessor end-to-end.

- **test_op_seqpat_step.py + test_op_palindrome.py** (`1c4511b7`)  
  New 320-second long-window experiments: `seqpat_step` asserts no silences > 5 s and RMS stability across halves; `palindrome` asserts all four expected pitches present in spectrum.

## docs/ — 4 files touched

- **New PRD: patterns as scalar values** (`0cc94d76`)  
  Adds `prd-patterns-as-scalar-values.md` (730 lines) covering `v"` / `n"` / `s"` / `c"` typed mini-notation prefixes, Pattern→Signal auto-coercion, explicit `scalar()` cast, and Phase 2.1 transforms with pattern args.

- **PRD patterns-as-scalar-values: review pass** (`e396b081`)  
  Renames `BuiltinDef` → `BuiltinInfo`, drops invalid `run(8)`/`binary(...)` mini-notation claims, adds §5.3.1 operator type rules (Pattern+Number stays Pattern; Pattern+Signal coerces; Pattern+Stereo → E165), new §9.0 Error Code Reference (E160–E165 + W170), and Phase G blocker doc/tutorial scope list.

- **Smooch wavetable PRD rewrite + follow-up** (`4e8ecae1`, `e62ee7da`)  
  Full rewrite of `prd-smooch-wavetable-synth.md` (445 insertions): fixes mip-level math, adopts Serum 2048-frame WAV convention, adds Akkado `wt_load` integration, fractional mip crossfading, peak-amplitude normalization, and a 12-case test strategy. Follow-up commit adds §5.3 listing Gaussian, Kaiser, Tukey, Lanczos, and additive resynthesis as band-limiting taper candidates.

- **Phase 2 PRD §11 + audit report** (`3d46118f`, `1c4511b7`)  
  Adds dedicated §11 to `prd-pattern-array-note-extensions-phase-2.md` calling out the two deferred follow-ups (custom-property accessor, standalone bend/aftertouch) with a recommended implementation order. New audit report `prd-pattern-array-note-extensions-phase-2_audit_2026-04-28.md` (101 lines) documents ship status and closed coverage gaps; notes PRD §10.1 table typo (algorithm correct, doc wrong).
