# Daily Summary: 2026-05-07

**Commits:** 10 | **Contributors:** Moritz Laass

---

## akkado/

**9 files touched** across 6 commits

- **feat(records): expose extended pattern event fields via %.field** (`19feea21`)  
  Ships PRD §3.1–§3.3 extended fields (note/midi/n, dur/chance, time/t0/start, phase/cycle/co, sample_id, freq aliases) that previously errored E136. Adds two new Cedar opcodes — `SEQPAT_FIELD` (rate-multiplexed scalar reader) and `SEQPAT_PHASE` (event-scoped 0..1 phasor). `PatternPayload::fields` grows from 5 → 11; `pattern_field_index()` and `available_fields()` now share a single registry so E136 stays accurate. (`typed_value.hpp`, `codegen_patterns.cpp`, `typed_value.cpp`, `test_codegen.cpp`)

- **fix: per-voice velocity in sample-pattern polyrhythms** (`f5bacfa7`)  
  `s"[cp, bd{vel:0.05}]"` was applying the bd velocity to all voices because `compile_polyrhythm_events` reduced to a single merged velocity. `Event`/`OutputEvent` now carry a `velocities[]` array parallel to `values[]`; `SamplerVoice` bakes velocity at trigger time; `compile_polyrhythm_events` drops the min-reduction. Pitched/value polyrhythms are unaffected. Adds a regression test for the reported case.

- **refactor(codegen): add emit_sample_chain helper + golden bytecode test** (`6d810385`)  
  Phase 1 of sample-emission unification PRD. Introduces `SamplePatternEmitCtx` and `emit_sample_chain`; adds `test_sample_emission_golden.cpp` with 10 representative shapes (G1–G10) asserting SAMPLE_PLAY input-wiring invariants to anchor subsequent phases.

- **refactor(codegen): route 6 class-method sites + scalar sample() through emit_sample_chain** (`64686f8e`)  
  Phase 2. Migrates all `emit_sampler_wrapper()` call sites in `codegen_patterns.cpp` (handle_mini_literal, handle_pattern_reference, handle_velocity_call, handle_bank_call, handle_variant_call, handle_transport_call) and the scalar `sample()` builtin to the unified helper. Moves `SamplePatternEmitCtx` to `akkado/include/akkado/codegen/helpers.hpp` as a templated inline.

- **refactor(codegen): unify atom property extraction (apply_atom_properties)** (`3604ae3d`)  
  Phase 3. Adds `SequenceCompiler::apply_atom_properties` as the single extraction point for record-suffix properties `{vel, dur, cutoff, bend, …}`. `BranchEvent` gains `prop_vals[]` and a `uint32_t` bitmap; polyrhythm merge uses bitmap-aware first-wins so explicit `{cutoff:0}` survives. Two regression tests added for custom-slot propagation and explicit-zero precedence.

- **refactor(codegen): delete inline mirror + emit_sampler_wrapper** (`08521171`)  
  Phase 4 (final). Removes the inline SAMPLE_PLAY mirror in `emit_pattern_with_state`, the "Keep both in sync" warning, `emit_sampler_wrapper` definition and declaration. `grep "Opcode::SAMPLE_PLAY" akkado/` now returns exactly one emission site.

---

## cedar/

**7 files touched** across 2 commits

- **feat(records): expose extended pattern event fields** (`19feea21`)  
  New opcodes `SEQPAT_FIELD` and `SEQPAT_PHASE` added to `sequencing.hpp`; `instruction.hpp` gains 2 enum entries; `opcode_metadata.hpp` auto-generated with 4 additions; `vm.cpp` dispatch updated.

- **fix: per-voice velocity in sample-pattern polyrhythms** (`f5bacfa7`)  
  `dsp_state.hpp` and `samplers.hpp` updated so `SamplerVoice` carries per-voice velocity; `sequence.hpp` extends `Event`/`OutputEvent` with `velocities[]`.

---

## tools/

**~12 files touched** (1 commit)

- **Auto-load default 808 sample kit in nkido-cli** (`313cf4d7`)  
  Bare sample names like `bd`, `hh`, `sd` that resolve out-of-the-box on the web were producing "sample not found" warnings in the CLI. Adds `asset_loader.cpp/hpp` with a discovery chain: `NKIDO_DEFAULT_KIT` env var → CMake macro → walk-up from CWD → install-relative fallback, using `web/static/samples/bpb_808_clean/strudel.json` as the canonical kit manifest. The default bank is appended last so user `--bank` flags always shadow it. `--no-default-bank` flag added for strict opt-out. 5 new tests (parity, user-precedence, opt-out, missing-kit graceful-degrade, render).

---

## web/

**2 files touched** (1 commit)

- **Auto-load default 808 sample kit** (`313cf4d7`)  
  `web/static/samples/bpb_808_clean/strudel.json` added (41-entry manifest mirroring `DEFAULT_DRUM_KIT`); `default-samples.ts` updated to align with the new canonical list.

---

## docs/

**4 files touched** across 4 commits

- **Add records system unification PRD (Phase 2)** (`c6c28091`)  
  New `docs/prd-records-system-unification.md` (1076 lines) specifying editor field-name autocomplete, standalone/function-parameter destructuring with defaults, the record-as-options convention, and a merged mutability design where record-valued state cells are the mechanism and `cell.field` / `cell.field = v` is bidirectional parser sugar.

- **Refine PRD: unify SAMPLE_PLAY codegen emission** (`b8979025`)  
  Corrects line ranges, replaces `MAX_PROP_SLOTS` with `cedar::MAX_PROPS_PER_EVENT`, widens `prop_vals_used` to `uint32_t`, specifies bitmap-aware polyrhythm merge, replaces ambiguous golden test cases G6/G9, adds G12 for bitmap-aware merge.

- **Mark sample-emission-unification and vscode-plugin PRDs as DONE** (`826b6c75`)  
  Status updated in `prd-sample-emission-unification.md` and `prd-vscode-plugin.md`.

- **feat(records) PRD update** (`19feea21`)  
  `prd-records-and-field-access.md` status flipped PARTIAL → DONE; voice and sample-as-String fields remain deferred per polyphony/numeric-Signal constraints.
