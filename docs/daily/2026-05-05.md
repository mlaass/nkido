# Daily Summary 2026-05-05

**Commits**: 9 (excluding 1 bot daily-summary and 1 empty merge)
**Contributors**: Moritz Laass

---

## tools/ — 3 commits, 24 files touched

- **Fix nkido-cli play/serve/ui silence for sample and poly patches** (`7f86a99`)
  Extracted shared asset-loading work into `program_loader.{hpp,cpp}` and routed all four execution modes (play/serve/ui/render) through it. Fixes silence in patches using `samples()` banks, soundfonts, wavetables, and polyphonic state inits — previously only render mode loaded these assets. Adds `resolve_sample_ids_in_events()` (mirroring the WASM path the CLI was missing entirely) and Catch2 integration tests covering three fixture patches.

- **Add interactive controls + scroll/scope views to nkido-cli serve** (`00a6d0be`)
  SDL serve window now has a play/stop button, master volume slider, and a Wave|Osc display toggle. Oscilloscope uses rising-zero-crossing trigger lock with auto-scale Y; waveform is a 5-second scrolling waterfall. Master gain applied atomically per-sample so meter matches output. Bumps `WAVEFORM_SIZE` 512 → 2048.

- **Honor patch BPM in nkido-cli + show it in serve toolbar** (`1cf1db7`)
  `apply_builtin_var_overrides()` added to `program_loader` and called from both the immediate-load and hot-swap paths. `--bpm` CLI flag is now optional (only applied when explicit). Live `ctx.bpm` surfaced in the serve toolbar between Wave/Osc toggle and volume slider.

## web/ — 3 commits, 6 files touched

- **Rework waveform as long-horizon view; auto-scale both viz to signal range** (`8d0246`)
  Waveform now maintains a sliding per-column min/max history over a configurable duration (default 5 s), scrolling waterfall-style. Both oscilloscope and waveform auto-scale the y-axis with a ±1 floor; signal exceeding ±1 overdraws in error colour. Refactors `oscilloscope.ts` and `waveform.ts`.

- **Tweak dnb-amen, effects-chain, and poly-chords demo patches** (`0012956`)
  `dnb-amen`: BPM 174 → 176, `fast(2)` → `fast(4)` on amen chop, saturate + limiter on drums, freeverb + saturate + limiter on bass, rebalanced mix. `effects-chain`: added `<a3 b3>` alternation and `ab4`, simplified output. `poly-chords`: added `limiter(@,-0.5)` to poly pad.

- **Fix /embed 404 on prod: hoist netlify.toml to repo root, restore prerender** (`148041b`)
  `web/netlify.toml` → `netlify.toml`; `publish` updated to `"web/build"`. Prerender re-enabled on `/embed` (disabled by `b97aa06`) so `embed.html` ships again; SPA fallback now acts as safety net for future routes.

## akkado/ — 1 commit, 1 file touched

- **Fix duplicate diagnostics from pattern-transform handlers** (`d3939ec`)
  `CodeGenerator::visit()` was returning a handler's `TypedValue` without caching it in `node_types_`. On re-visit (e.g. the channel-type check loop) the dispatcher cache-missed, re-ran the handler, and emitted the same diagnostic twice. A single defensive cache write at the dispatcher entry fixes duplication across all ~30 special handlers.

## docs/ — 2 commits, 4 files touched

- **Audit prd-records-and-field-access: PRD status DONE → PARTIAL** (`f58e9ae`)
  §1–2 and §4–6 ship; §3 extended pattern fields (`%.note`, `%.dur`, `%.chance`, `%.time`, `%.phase`, `%.voice`, `%.sample_id` and aliases) are unimplemented — accessing them errors E136. Status rolled back to PARTIAL. Broken `%.note` examples in `conditionals.md` replaced with working `%.freq` equivalents.

- **Add PRD: YouTube/video audio import via samples() with clips dict** (`52c9cd1`)
  801-line PRD for a future feature allowing YouTube and general video URLs as sample sources via a `clips` dictionary in `samples()`, covering URI handling, clip extraction, caching, and language-level design.
