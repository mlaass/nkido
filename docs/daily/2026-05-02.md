# Daily Summary: 2026-05-02

**Commits:** 22 (including 1 merge)  
**Contributors:** Moritz Laass  
**Period:** 2026-05-01 13:14 UTC+2 – 2026-05-02 00:30 UTC+2

---

## cedar/ — 5 files touched

- **SoundFont audit: close coverage gaps** (`3da4fdf`) — Adds `test_soundfont_registry.cpp` (Catch2 coverage for `SoundFontRegistry::load_from_memory` dedup, invalid-input rejection) and exposes the SoundFont surface on `cedar_core` bindings (`soundfont_load_from_file/memory`, `soundfont_count`, `soundfont_presets_json`, `SOUNDFONT_VOICE` enum). Flips PRD status to COMPLETE.
- **Userspace-state audit: edge/state coverage** (`7d5977c`) — Adds `test_edge_op.cpp` and `test_state_op.cpp` with 8 new Catch2 cases covering `step()` variants, `gateup(constant)` pulse, uninitialized `s.get()`, multi-site closure independence, empty-array `step`, hot-swap state preservation, and `STATE_OP rate=2` semantics.

---

## akkado/ — 5 files touched

- **Fix pattern transforms on identifier-bound patterns** (`5815d9a`) — `compile_pattern_for_transform()` had no `Identifier` case, causing `E130` for patterns bound via `melody = n"…"; melody |> transpose(…)`. Adds recursive resolution through `Symbol::pattern.pattern_node`, fixing all 24 transform builtins routed through that helper.
- **Add `spb` (seconds per beat) as read-only builtin variable** (`8a38f5e`) — Registers `get_spb` in `BUILTIN_FUNCTIONS`, adds `spb` to `BUILTIN_VARIABLES`, emits `ENV_GET(__spb)` on read, with tests for read-only enforcement.
- **Audit PRD timeline-curve-notation: edge case tests** (`3cbce3e`) — 8 new `test_codegen.cpp` cases (empty curve, single-char, ramp at start/end, `~` on ramp, breakpoint-limit truncation).
- **Userspace-state audit: akkado test coverage** (`7d5977c`) — Adds `test_state.cpp` with 5 akkado-side cases (closure multi-site state, `[].step(trig)` silence-not-crash, `STATE_OP` hot-swap with changed init).

---

## web/ — ~57 files touched

### Embed route
- **Add 10 landing-page demo patches and embed postMessage handler** (`bd0c74e`) — Adds 9 new `.akk` patches (`drum-machine`, `effects-chain`, `fm-piano`, `interactive-params`, `microtonal-raga`, `poly-chords`, `soundfont-play`, `visualizations`, `wavetable-scan`) with `landing: true` in `patches/index.json`. Embed gains `nkido:switch-patch` postMessage listener and posts `nkido:embed-ready` on boot.
- **Simplify embed patch-switch handler and add `?autoplay=1` flag** (`1de64db`) — Strips bidirectional ack protocol and 100ms remount kludge down to `applyPatch + evaluate` with try/catch. `?autoplay=1` runs `evaluate()` after initial patch load.
- **Fix embed patch loading with proper error handling** (`d59efea`) — Adds error handling to the embed boot path.

### SoundFont / audio
- **Fix silent audio when SoundFont URL fallback should kick in** (`03e7049`) — `loadSoundFontFromUrl` returns `null` on failure rather than throwing; the compile loop now checks the return value, enabling `.sf3 → .sf2` fallback. Also gates optional `gm_medium`/`gm_large` downloads in `download-soundfonts.sh` behind `--minimal`.
- **Install sf3convert in CI to ship smaller SF3 SoundFont** (`c6aa3c9`) — Adds `sf3convert` to `.github/workflows/deploy.yml`, reducing deployed SoundFont from ~5.7 MB to ~2.6 MB.
- **Update soundfont-play demo** (`9e6a507`, `7d7f5c4`, `7edc6b3`) — Switches `pat()` to `n"..."` notation, minor melody (eb4/f#4), `@`-suffix timing, reduced reverb mix; adds bass layer with dattorro reverb and stereo panning.

### Patches
- **Refactor landing demo patches** (`c04d447`) — Updates `fm-piano`, `microtonal-raga`, `poly-chords`, `visualizations`, `wavetable-scan` to use `n"..."` notation, `@.freq/@.trig` destructuring, `wt()` alias.
- **Update drum-machine and effects-chain demos** (`ccb8639`) — Replaces `%` with `@`, switches to `n"..."` notation, adjusts effect parameters (saturate drive 2→5, lp cutoff 220→420.69), adds LFO-modulated aux output in effects-chain.

### Docs
- **Humanize reference and tutorial docs** (`e583357`) — Sentence-case headings, removes AI phrasing and em-dashes across 33 files (language reference, mini-notation reference, tutorials 01–06).
- **Add curve-notation reference page** (`3cbce3e`) — Creates `web/static/docs/reference/mini-notation/curve-notation.md`; docs manifest grows to 35 entries.

---

## experiments/ — 1 file touched

- **SoundFont Python experiment** (`3da4fdf`) — `test_op_soundfont.py` validates retrigger fix end-to-end (gate-edge, sustained-gate sequential notes, re-articulation, note-change fallback, velocity scaling, vibrato, gate-off). Includes `--soak` mode (200 loops × 1.5 s = 300 s simulated audio).

---

## docs/ — 7 files touched

- **VS Code extension PRD** (`2253ac3`) — Adds `docs/prd-vscode-plugin.md` (715 lines) defining a Direct API architecture and CLI integration for an Akkado VS Code extension; removes the superseded plan file.
- **Audit reports** (`3da4fdf`, `3cbce3e`, `7d5977c`) — Three new audit reports under `docs/audits/` covering SoundFont playback fixes, timeline curve notation, and userspace state/edge primitives — all flipped to COMPLETE.
- **MVP incomplete-implementations note** (`5815d9a`) — Documents the pattern-transform identifier-binding gap.

---

## other — 5 files touched

- **Release v0.3.0** (`3c38dba`) — Bumps `VERSION` and `web/package.json`.
- **Add 0.3.0 changelog entry** (`96fd40a`) — Adds CHANGELOG.md entry.
- **Add Hippocratic Code of Conduct** (`2b6a4a0`) — Adds `CODE_OF_CONDUCT.md`.
- **Link CONTRIBUTING to Code of Conduct** (`37ec3c8`) — Cross-links `CONTRIBUTING.md`.
