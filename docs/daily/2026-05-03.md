# Daily Summary 2026-05-03

**Commits:** 21 (20 feature + 1 merge)  
**Contributors:** Moritz Laass  
**Period:** 2026-05-02 12:39 UTC+2 – 2026-05-03 00:42 UTC+2

---

## cedar/

**Files touched: ~25**

- **URI resolver phase 1** (`363b005`): Added `cedar::UriResolver` with scheme-keyed dispatch, `FileHandler` (file://), and `BundledHandler` (bundled://). `LoadResult` moved out from under the Emscripten guard so the header is usable on WASM. 10 tests / 51 assertions added under `[uri-resolver]`.
- **URI resolver phase 2** (`934489016`): Added `HttpHandler` (OpenSSL-backed HTTPS, 30s timeout, 5-redirect follow), `GithubHandler` (raw.githubusercontent.com URL transform), and `cedar::FileCache` (500MB LRU disk cache, XDG-aware paths, FNV-1a-64 hex filenames). Vendored cpp-httplib v0.18.5 (header-only, MIT) under `third_party/`.
- **URI resolver phase 4** (`fea1d13`): Collapsed `SoundFontRegistry::load_from_memory` and `WavetableBankRegistry` raw-pointer overloads onto `MemoryView`. Deleted `SampleBank::load_wav_file` and `load_wav_memory`; `load_audio_data` is now the sole memory ingestion path. Cedar: 184 passing / 334,848 assertions green.
- **URI resolver phase 9 fix** (`b6c7028`, minor): Patched orphan `bank.load_wav_file` call in `sample_pack.hpp`, routing it through `detail::load_sample_file` backed by `FileLoader + load_audio_data`.

---

## akkado/

**Files touched: ~21**

- **Microtonal PRD closure** (`1c084ee`): Added `Kind::JI` (5-limit symmetric 12-tone, C4-anchored) and `Kind::BP` (Bohlen-Pierce 13edt) tuning contexts. Lexed `d` (Stein-Zimmermann inverted-flat) and `\` (alt-down) micro-aliases with forward-lookahead to keep `bd`/`sd` sample names parsing correctly. Descoped `^n`/`vn` numeric-argument syntax (irreconcilable with octave digits). Added 216 lines of new tests; PRD flipped to COMPLETE. Full audit report at `docs/audits/prd-microtonal-extension_audit_2026-05-02.md`.
- **URI resolver phase 7: `samples()` builtin** (`58ce180`): Added `UriKind` enum and `UriRequest` struct to `codegen.hpp`; threaded `required_uris` through `CodeGenResult`, `CompileResult`, and WASM bridge. Registered `samples("uri")` as a top-level NOP builtin that validates a single string-literal arg, deduplicates identical URIs, and appends a `SampleBank` request to the compile result — no audio-time instruction emitted. 7 test cases / 23 assertions.
- **Remove `post()` syntax** (`75b8d1c`): Deleted undocumented `post()` statement from lexer keyword table, `TokenType`, `NodeType`, parser dispatch + `parse_post_stmt()`, codegen visit case, spec grammar, and tests. The construct had no documented semantics and zero shipping uses.

---

## web/

**Files touched: ~40**

- **URI resolver phase 3: TS resolver** (`e306273`): New `web/src/lib/io/uri-resolver.ts` with scheme-keyed dispatch mirroring the C++ side. Six default handlers: http/https (fetch + IndexedDB cache), github (URL transform + recurse), blob (transient File/ArrayBuffer), bundled (build-time table), idb (direct IDB key read). `loadFile()` collapsed to one signature; `FileSource` discriminated union deleted. 19 tests in `web/tests/uri-resolver.test.ts`.
- **URI resolver phase 5: WASM bridge standardization** (`8b9f34f`): Reordered `cedar_load_soundfont` params to `(name, data, size)` to match other loaders. Standardized all four WASM loaders on `int32_t` return with `-1` on failure. Dropped dead `_cedar_load_sample_wav` from exports list.
- **URI resolver phase 6: audio store collapse** (`0004b2b`): Deleted `loadSampleFromUrl`, `loadSoundFontFromUrl`, `loadWavetableFromUrl`, and `loadBankFromGitHub` from the audio store and `BankRegistry`. Replaced with one overloaded `loadAsset(uri, kind, name)` covering all nine call sites. Added `web/tests/bank-registry.test.ts` pinning the single-fetch invariant.
- **Split per-feature docs** (`74f1c84`): Authored 11 new reference pages: `fm-synthesis`, `samplers`, `soundfonts`, `samples-loading`, `polyphony`, `timelines`, `oscilloscope`, `waveform`, `spectrum`, `waterfall`, `pianoroll` (builtins); `chords` (mini-notation). Refactored `sequencing.md` and `visualizations.md` into section overviews with cross-links. Docs manifest regenerated to 48 docs / 499 lookup entries.
- **Humanize per-feature docs** (`608b6ae`): Replaced em dashes with colons/semicolons/periods and rewrote passive openers across 14 split docs.
- **dnb-amen patch** (`046b70a`): Added `web/static/patches/dnb-amen.akk` (Amen-break drum'n'bass demo using `samples("github:tidalcycles/Dirt-Samples")`); registered in patch index.

---

## tools/

**Files touched: ~8**

- **URI resolver phase 8: CLI URI flags** (`9848127`): Wired `nkido-cli` through the URI resolver: registers `FileHandler`, `HttpHandler`, `GithubHandler`, `BundledHandler` at startup. New `--bank`, `--soundfont`, `--sample` flags accept any registered URI scheme. New `tools/nkido-cli/asset_loader.cpp` includes a minimal strudel.json manifest scanner covering `_base`/`_name` and string/string-array fields. `akkado-cli` gains `--uris` (text + JSON) listing `required_uris`. Docs manifest regenerated to 36 docs / 419 entries.
- **Named-bank support** (`046b70a`): `nkido-cli` now derives a bank name from each `--bank`/`samples()` URI (matching the web `BankRegistry.extractBankName` heuristic) and registers it in `named_banks` so `.bank("Name")`-tagged events resolve identically on the CLI.

---

## docs/

**Files touched: ~14**

- **URI resolver PRD** (`09ceb62`): New `docs/prd-uri-resolver.md` (800 lines) combining the protocol-resolver design with the akkado HTTP samples follow-up. Tracks phases 1-9; §1 flipped to DONE in phase 9.
- **Shareable patches PRD** (`2480def`): New `docs/prd-shareable-patches.md` (819 lines): anonymous shareable patches with multi-draft local persistence, Cloudflare Worker + D1 backend at share.nkido.cc, and a `StorageProvider` interface for future SaaS layering.
- **Error-handling PRD realignment** (`ed52894`): Fixed call-site line numbers, corrected E160-E184 collision range, closed gaps around partial-AST robustness, dedup rule, token-display helper, and worklet crash recovery scope.
- **File-loading review report** (`657ddad`): New `docs/reports/2026-05-02-file-loading-review.md` (550 lines) surveying redundant load entry points across C++/WASM/TS layers; recommended the URI resolver approach.
- **Docs cleanup** (`64904332`, `69e27bbb`): Moved `MVP-INCOMPLETE-IMPLEMENTATIONS.md` to `docs/reports/2026-01-31-mvp-incomplete-implementations.md`; updated stale notes and added §4.1 "Known Non-MVP Gaps" documenting E105 array expression defaults.
- **Microtonal audit report** (`1c084ee`): New `docs/audits/prd-microtonal-extension_audit_2026-05-02.md`; PRD rewritten to reflect shipped reality.
- **URI schemes doc** (`9848127`, `046b70a`): New `docs/uri-schemes.md` covering scheme list, `samples()` syntax, CLI usage, and caching layout; cross-linked from mini-notation reference.
- **CHANGELOG** (`b6c7028`): Added `[Unreleased]` entry covering the full URI resolver feature set.
