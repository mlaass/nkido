# Daily Summary — 2026-04-26

**Commits:** 15  
**Contributors:** Moritz Laass  
**Period:** 2026-04-25 13:42 UTC+2 → 2026-04-26 00:17 UTC+2

---

## cedar/ — 7 files touched

- **Fix `poly()` cycle-alignment bugs at rational BPMs** (`cedar/src/vm/vm.cpp`): Three independent bugs fixed that surfaced when chord transitions land on exact block boundaries (e.g. BPM 110 / 48 kHz / 128-sample block). (1) Wrap-detection gate-off replaced epsilon hack with `evt_end >= cycle_length && current_cycle > 0` branch. (2) IEEE-754 precision: snap `cycle_pos` to 0 within 1e-9 beats to prevent `fmod` returning near-zero noise. (3) Envelope retrigger: force `gate_buf[0]=0` when `pending_gate_on==0` so shared-frequency voices get a rising edge at sample 1. All chord RMS ratios now >0.96 (was ~0.55).
- **Hot-swap reliability — new tests** (`cedar/tests/test_hot_swap.cpp`, `CMakeLists.txt`): Covers literal constant propagation, `dag_hash` differs/matches as expected, and rapid sequential loads all eventually applying — the contract the AudioWorklet relies on.
- **`poly()` polish — stateful opcode metadata and regression test** (`cedar/include/cedar/generated/opcode_metadata.hpp`, `cedar/include/cedar/opcodes/dsp_state.hpp`, `cedar/tests/test_poly.cpp`): Mark `POLY_BEGIN` stateful so the disassembly inspector lights up; new `test_poly.cpp` runs 32 cycles × 4 chords and asserts 127 onsets, 0 incomplete.
- **Close prd-conditionals-logic** (`cedar/tests/test_vm.cpp`): 4 new `[opcodes][logic]` cases for `SELECT`, all `CMP_*`, and all `LOGIC_*` opcodes with epsilon coverage guarded by `static_assert(LOGIC_EPSILON == 1e-6f)`.

---

## akkado/ — 11 files touched

- **`poly()` signature reorder + voice ceiling** (`akkado/include/akkado/builtins.hpp`, `akkado/src/codegen_functions.cpp`, `akkado/tests/test_codegen.cpp`): New signature `poly(input, instrument, voices=64)` — instrument adjacent to input, voices optional (1–128). Breaking change; `MAX_VOICES` raised 32 → 128. Regression test compiles the chord-stab patch and verifies 0 incomplete onsets.
- **Arrays: `fold` → `reduce` rename, empty array `[]`, full test suite** (`akkado/src/codegen_arrays.cpp`, `codegen.cpp`, `analyzer.cpp`, `builtins.hpp`, `codegen.hpp`, `const_eval.cpp`, `tests/test_arrays.cpp`, `tests/test_codegen.cpp`, `tests/test_const_eval.cpp`, `tests/CMakeLists.txt`): Rename higher-order `fold` → `reduce` to stop it silently shadowing the wavefolder `fold` builtin. Drop `product` (expressible as `reduce`). Empty `[]` now produces a zero-length `Array TypedValue`. New `test_arrays.cpp` covers literals, negative/OOB wrap indexing, and every array builtin.
- **Close prd-conditionals-logic** (`akkado/tests/test_codegen.cpp`): 6 new `[conditionals][runtime]` cases asserting output buffer values for truth tables, epsilon equality (0.1+0.2==0.3), negative-falsy `select`, infix/function-call equivalence, and operator precedence.

---

## web/ — 17 files touched

- **New `PolyStateInspector.svelte`** (`web/src/lib/components/Panel/PolyStateInspector.svelte`, `StateInspector.svelte`): Live voice grid rendering note, freq, vel, gate dot, and age — routed from `StateInspector` when `stateData.type === 'PolyAllocState'`. Opcode-metadata generator updated to skip NOP-mapped builtins and explicitly mark `POLY_BEGIN`.
- **`poly/mono/legato/spread` in editor builtins list** (`web/src/lib/editor/generated/syntax-builtins.ts`): Regenerated from `builtins.hpp` so autocomplete covers polyphony operators.
- **Freeze step-highlighting on edited line until recompile** (`web/src/lib/editor/step-highlight.ts`, `web/src/lib/stores/pattern-highlight.svelte.ts`): Track `frozenOffset` — on every `docChanged` transaction lower it to the line-start of the first changed range; polling skips patterns at or after that offset. Clears on successful compile.
- **Hot-swap reliability: SlotBusy retry in AudioWorklet** (`web/src/lib/stores/audio.svelte.ts`, `web/src/lib/components/Editor/Editor.svelte`, `web/static/worklet/cedar-processor.js`): Retry moved from JS polling (5×20 ms, console-only failure) into the AudioWorklet `process()` callback. `refreshId` tagging lets overlapping Ctrl+Enter presses resolve their own promises. Refresh-pulse animation on "Ready" status provides visible feedback for every press.
- **Arrays reference documentation** (`web/static/docs/reference/language/arrays.md`, `web/src/lib/docs/manifest.ts`): New `arrays.md` with F1-anchored per-function lookup (`index_headings: true`); manifest updated.
- **Conditionals docs, operators, and rhythm tutorial** (`web/static/docs/reference/language/conditionals.md`, `web/static/docs/reference/language/operators.md`, `web/static/docs/tutorials/04-rhythm.md`, `web/scripts/build-docs-index.ts`): New `conditionals.md` function reference with per-function F1 anchors; `operators.md` extended with comparison/logic/conditional sections and full desugar map; `04-rhythm.md` gets a "Conditional Triggers" section; `build-docs-index.ts` adds opt-in `index_headings` frontmatter flag.
- **F1 docs for `poly/mono/legato/spread`** (`web/static/docs/reference/builtins/sequencing.md`): Full reference entries added.
- **`chord-stab.akk` patch** (`web/static/patches/chord-stab.akk`): Updated to use `@` holes and adds a delay tail (updated twice across commits).

---

## experiments/ — 1 file touched

- **`test_op_poly.py` — render-mode polyphony experiment, extended to 300 s** (`experiments/test_op_poly.py`): Initial version drives the chord-stab patch through `nkido-cli render`, reads WAV + JSONL voice trace, and checks: voice grid sanity (firing ≤ 3), audio quality (clicks/DC/RMS spikes), and voice churn (releasing voices clear within 12 blocks). Extended to 300 s at BPM 110 with hard assertions for all three cycle-alignment bug regressions (firing < 3, firing > 3, per-chord RMS ratio min/mean ≥ 0.7 — now all >0.96).

---

## tools/ — 2 files touched

- **`nkido-cli` render mode** (`tools/nkido-cli/main.cpp`, `tools/nkido-cli/bytecode_loader.hpp`): New offline WAV rendering subcommand. Compiles Akkado source, applies `state_inits` (sequence/poly/timeline) as in the WASM path, runs the VM for `--seconds` at `--bpm`, writes 16-bit stereo WAV to `-o`. `--trace-poly` writes per-block `PolyAllocState` voice grid as JSONL. Heap-allocates VM to avoid stack overflow.

---

## docs/ — 7 files touched

- **New PRD: `prd-pattern-event-arrays.md`** (493 lines): Surfaces `OutputEvent.values[]` as dynamic-array `TypedValues` via `notes(e)` and `freqs(e)` builtins; makes `len()` polymorphic over static/dynamic arrays; removes the unused `ChordLit` (`'`-suffix) syntax. Depends on userspace-state PRD.
- **New PRD: `prd-unison.md`** (553 lines): Ships `unison()` as an Akkado stdlib function using existing closures, `linspace`, `pan`, and oscillator phase params. Only compiler-side change: extend `map(arr, fn)` to accept 2-arg `(val, idx)` closures.
- **New + refined PRD: `prd-userspace-state-and-edge-primitives.md`**: Initial 621-line PRD added then refined — resolves open questions Q1/Q4: cell I/O ships as one mode-dispatched `STATE_OP` opcode (`init/load/store` via `inst.rate`); `state/get/set` are reserved identifiers; UFCS method-call codegen specified.
- **Bug report: `docs/reports/2026-04-25-poly-cycle-alignment-bugs.md`** (258 lines): Full root-cause analysis of the three cycle-alignment bugs with patterns to watch for in similar issues.
- **`prd-polyphony-system.md`**: Phases 0–4 marked shipped; all `poly(n, ...)`/`mono(...)` references updated to new `poly(%, n, ...)`/`mono(%, ...)` signatures; Phases 5–6 (polish + chord-completeness investigation) added.
- **`prd-conditionals-logic.md`**: All Phase 1–5 implementation checklist boxes ticked.
- **`docs/vision-language-evolution.md`**: Updated to reflect `fold` → `reduce` rename.

---

## other/ — 2 files touched

- **`CLAUDE.md`**: New guideline #6 — sequenced opcode tests must simulate ≥300 s of audio to surface "every few bars" bugs.
- **`.gitignore`**: Ignore `.claude/*.lock` files.
