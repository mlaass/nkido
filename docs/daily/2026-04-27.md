# Daily Summary — 2026-04-27

**Commits:** 9 (excluding merge)  
**Contributors:** Moritz Laass

---

## cedar/

**Files touched:** 10

- **Fix phaser: dry+wet sum + expose stages and feedback** (`299af3f`)  
  The phaser was producing no audible effect — it emitted only the all-pass cascade, which has unity magnitude response. Fixed to sum `input + allpass_cascade` (canonical Bode/MXR topology). Stages and feedback are now optional Akkado compile-time params packed into `inst.rate`. Defaults: stages=4, feedback≈0.5. New Catch2 smoke test asserts ≥6 dB notch depth; rewritten Python experiment runs ≥300 s.

- **Add live audio input: `in()` builtin + INPUT opcode + host wiring** (`629a650`)  
  New `INPUT` opcode (stateless memcpy) with `input_left`/`input_right` pointers on `ExecutionContext` and `VM::set_input_buffers()` API. Silent fallback when pointers are null. New tests in `test_vm.cpp`.

---

## akkado/

**Files touched:** 14

- **Extend array utility builtins with optional params** (`6bad83b`)  
  Six builtins gain optional parameters, all backwards-compatible: `sort(reverse=false)`, `shuffle(seed=auto)`, `random(min=0, max=1)`, `normalize(lo=0, hi=1)`, `harmonics(ratio=1.0)`, `linspace(mode="linear")` with new `"log"` and `"geom"` modes. Also extends `resolve_const_scalar()` to accept `BoolLit`.

- **Add optional step parameter to `range()`** (`daaeb16`)  
  Default step is 1; direction is still auto-determined from start vs end, preserving existing `range(N, 0)` reverse behavior.

- **Fix phaser: expose stages/feedback as Akkado params** (`299af3f`)  
  New codegen handler in `codegen.cpp` packs stages and feedback into `inst.rate` at compile time.

- **Add live audio input: `in()` builtin** (`629a650`)  
  `in()` builtin with optional compile-time string source (`"mic"`, `"tab"`, `"file:NAME"`); allocates an adjacent stereo buffer pair so mono DSP auto-lifts. New codegen in `codegen_patterns.cpp`; tests in `test_codegen.cpp`.

---

## web/

**Files touched:** 16

- **Extend array builtins docs** (`6bad83b`, `daaeb16`)  
  `arrays.md` updated to document all new optional parameters across `sort`, `shuffle`, `random`, `normalize`, `harmonics`, `linspace`, and `range`.

- **Phaser modulation docs** (`299af3f`)  
  `modulation.md` updated with the corrected phaser signature (`stages`, `feedback` params) and expected +6 dB peak-gain behavior note.

- **Fix audio input panel "Audio not initialized" before play** (`c8ec8a9`)  
  `setInputSource` bailed silently when `audioContext`/`workletNode` were null (the state before `play()` runs). Lazy-initializes the engine in `setInputSource` so the user's button click creates the `AudioContext` on first use.

- **Add live audio input: web UI + WASM + worklet** (`629a650`)  
  New `AudioInputPanel.svelte` in the Settings tab with source selector, device picker, file upload, and `getUserMedia` constraint toggles. `audio.svelte.ts` extended with input-source management. `cedar-processor.js` AudioWorklet copies `inputs[0]` into WASM heap each block. WASM exports `cedar_get_input_left/right` + `cedar_set_input_active`. New `audio-input.md` reference page; docs manifest updated.

- **Complete Phase 6 docs for advanced-functions PRD** (`246852e`)  
  `closures.md` extended with factory functions, partial application, and composition sections. `manifest.ts` updated; PRD audit resolved to COMPLETE (one gap tracked as §7.6 future extension).

---

## experiments/

**Files touched:** 1

- **Rewrite phaser experiment** (`299af3f`)  
  `test_op_phaser.py` rewritten to run ≥300 s, assert ≥1 octave sweep with ≥12 dB median notch depth, and verify stationary-notch depth. Replaced the previous WAV-only "listen for notches" approach with programmatic spectral assertions.

---

## tools/

**Files touched:** 4

- **Add live audio input: SDL2 capture in CLI** (`629a650`)  
  `audio_engine.cpp/hpp` extended with SDL2 capture device support. New `--list-devices` and `--input-device` flags in `main.cpp`. Lock-free SPSC ring buffer bridges capture and playback callbacks.

---

## docs/

**Files touched:** 10

- **Merge previous daily summary** (`65ec7f2`)  
  `docs/daily/2026-04-26.md` merged in from remote.

- **Audio input PRD audit (2026-04-26)** (`c8ec8a9`)  
  Fresh audit document covering the shipped `in()` implementation and the `setInputSource` lazy-init fix; replaces the 2026-04-24 pre-implementation audit.

- **Add Phase 2 PRD for pattern array/note extensions** (`45f89dd`)  
  Scopes four unmet goals from the 2026-04-24 audit (voicing, extended note properties, time/structure modifiers, algorithmic generators) into a new follow-up PRD with four staged sub-phases (A–D). Original PRD updated with defer markers.

- **Apply PRD review fixes to pattern-array-note-extensions-phase-2** (`00ae5ac`)  
  Corrects type references (`cedar::SequenceState`), file paths, promotes custom-property field access to its own subsection, clarifies `iter`/`iterBack` time-origin formula, and fixes builtin count (22, not 23).

- **Complete Phase 6 docs for advanced-functions PRD; resolve audit** (`246852e`)  
  PRD §2.3 updated to reflect actual `resolve_function_arg()` reuse. §7.6 added for unenforced string-default error. Audit annotated with RESOLVED/DEFERRED markers; verdict upgraded to COMPLETE.

- **Add signal-rate square-wave test for conditionals; resolve audit** (`34d177b`)  
  Closes last open finding from conditionals/logic PRD audit: `osc("sin", 1) > 0` square-wave test (400 blocks, asserts every sample is exactly 0.0 or 1.0, ~50% duty cycle, ≥2 transitions). Audit annotated with Resolution section.
