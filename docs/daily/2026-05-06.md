# Daily Summary: 2026-05-06

**Commits**: 3 | **Contributors**: Moritz Laass

## akkado/ — 5 files touched

- **Fix mini-notation record-suffix in s"..." sample mode and chord atoms**
  (`b4aeef44`) — `lex_sample_only()` and `try_lex_chord_symbol()` never called
  `try_lex_record_suffix()`, causing `bd{vel:0.5}` and `Am{vel:0.5}` to
  mis-parse as polymeter syntax. Missing calls added to both paths; orphaned
  `lex_pitch_or_sample()` deleted. Tests cover sample atoms in `pat()` and
  `s"..."`, bracket groups, chord record-suffixes, whitespace tolerance, and
  polymeter regression. — `mini_lexer.hpp`, `mini_lexer.cpp`,
  `tests/test_codegen.cpp`

- **Apply sample-velocity attenuation in s"..." patterns** (`05c1150`) — Two
  independent bugs silenced `{vel:V}` on sample atoms: the polyrhythm flatten
  path (`flatten_to_timelines()`) discarded `ad.properties` so `{vel:0.25}`
  was lost; and `SAMPLE_PLAY` itself never multiplied output by velocity.
  Fix: property-extraction loop mirrored into `compile_polyrhythm_events()`;
  velocity applied as a `MUL` post-multiply emitted by `emit_sampler_wrapper()`
  rather than widening the opcode. All six call sites updated; 4 new `[velocity]`
  test cases added. — `codegen.hpp`, `codegen_patterns.cpp`,
  `tests/test_codegen.cpp`

## tools/ — 3 files touched

- **Apply sample-velocity attenuation** (`05c1150`) — Adds `load_bank` JSON
  command to `nkido-cli serve` mode so VS Code extension clients can register
  sample manifests at runtime without `--bank` flags. Backed by 4 integration
  tests covering registration, post-load resolution, missing-bank warning
  preservation, and bad-URI error handling. — `serve_mode.cpp`,
  `tests/CMakeLists.txt`, `tests/test_serve.cpp`

## web/ — 1 file touched

- **Apply sample-velocity attenuation** (`05c1150`) — Adds end-to-end Vitest
  test (`sample-velocity.test.ts`) loading the actual `nkido.wasm` artifact,
  registering a synthesized impulse sample, calling `cedar_process_block`, and
  asserting the peak-ratio against a reference — closing the gap where CLI
  render mode was the only e2e check. — `tests/sample-velocity.test.ts`

## docs/ — 1 file touched

- **Add PRD: DaisySP integration** (`4a0f473`) — New product requirements
  document for vendoring MIT-licensed DaisySP modules into Cedar as opcodes
  (drums, physical modeling, additive/FM voices, noise variants, pitch shifter,
  granular, looper) under a `ds_` prefix. LGPL submodule explicitly excluded.
  Five-phase rollout plan with WASM bundle cap specified. —
  `docs/prd-daisysp-integration.md`
